import {
	describe,
	expect,
	test,
} from '@jest/globals';

import { useCustomPreactHook } from './useCustomPreactHook.js';

describe('useCustomPreactHook', () => {
	test('example test', () => {
		expect(useCustomPreactHook).toBeTruthy();
	});
});
